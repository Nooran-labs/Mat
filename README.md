# Mat
This project is a C++ implementation of a square matrix class (`SquareMat`) without using the STL.   It supports core matrix operations, arithmetic, and logical features using manually allocated 2D arrays and operator overloading.
